apiVersion: v1
kind: Service
metadata:
  labels:
    name: employee-service  
    Namespace: "default"
spec:
  ports:
  - name: http
    protocol: TCP
    port: 8082     
  selector:
    app: employee-service
  type: LoadBalancer
